name: flutter_tensorflow_audio
description: Sua Musica Talks

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  #UI
  cupertino_icons: ^1.0.2
  lottie: ^1.2.2
  #Image Rec
  tflite : ^1.1.2
  camera: ^0.9.4+14
  #Audio Rec
  tflite_audio: ^0.2.2+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/labels.txt
    - assets/audio/soundclassifier_with_metadata.tflite
    - assets/image/labels.txt
    - assets/image/model_unquant.tflite
    - assets/ui/home_animation.json

